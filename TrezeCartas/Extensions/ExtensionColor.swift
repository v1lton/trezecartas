//
//  ExtensionColor.swift
//  TrezeCartas
//
//  Created by Evaldo Garcia de Souza Júnior on 08/02/21.
//

import Foundation
import SwiftUI
import UIKit

extension Color {
    static let pretoColor = Color(UIColor(named:"preto")!)
    static let brancoColor = Color(UIColor(named:"branco")!)
    static let roxoColor = Color(UIColor(named:"roxo")!)
    static let azulColor = Color(UIColor(named:"azul")!)
    static let amareloColor = Color(UIColor(named:"amarelo")!)
    static let rosaColor = Color(UIColor(named:"rosa")!)
    static let roxoClaroColor = Color(UIColor(named:"roxoClaro")!)
    static let pretoEscuroColor = Color(UIColor(named:"pretoEscuro")!)
}
